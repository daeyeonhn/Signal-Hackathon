# Server-only configuration. Never put secrets in a browser-exposed variable.
AUTH_BASE_URL=http://localhost:3000
AUTH_SECRET=

# SHA-256 digest of a random first-organizer setup code.
# Generate a code and its hash with: node scripts/create-organizer-code.mjs
ORGANIZER_SETUP_HASH=
